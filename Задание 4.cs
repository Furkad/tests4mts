class Program
{
    static void Main(string[] args)
    {

    }

    /// <summary>
    /// ¬озвращает отсортированный по возрастанию поток чисел
    /// </summary>
    /// <param name="inputStream">ѕоток чисел от 0 до maxValue. ƒлина потока не превышает миллиарда чисел.</param>
    /// <param name="sortFactor">‘актор упор€доченности потока. Ќеотрицательное число. ≈сли в потоке встретилось число x, то в нЄм больше не встрет€тс€ числа меньше, чем (x - sortFactor).</param>
    /// <param name="maxValue">ћаксимально возможное значение чисел в потоке. Ќеотрицательное число, не превышающее 2000.</param>
    /// <returns>ќтсортированный по возрастанию поток чисел.</returns>
    static IEnumerable<int> Sort(IEnumerable<int> inputStream, int sortFactor, int maxValue)
    {
        //не смог решить
    }
}